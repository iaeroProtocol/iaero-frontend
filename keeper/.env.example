# Copy this to `.env` and fill in real values. NEVER commit .env.
PRIVATE_KEY=0x...                              # keeper EOA (must hold KEEPER_ROLE on the vault)
RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_KEY
ZERO_EX_API_KEY=                               # https://0x.org/docs

# Optional — defaults shown
# FINALIZE=1
# MIN_USDC_PCT=95
# MAX_SWEEPS=3
# EXECUTION_BATCH_SIZE=10
# SKIP_INDIVIDUAL_RETRY=0
# DRY_RUN=0
# TARGET_EPOCH=
# FORCE_HIGH_SLIPPAGE=0      # rehearsal-only; do NOT set in production
